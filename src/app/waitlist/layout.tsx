// The waitlist page is a client component, so its metadata lives here.
export const metadata = {
  title: "Airdrop — Other World Projex",
  description:
    "The $RIBBIT airdrop waitlist — hold the required $RIBBIT, take a numbered place, and bring friends by referral.",
};

export default function WaitlistLayout({ children }: { children: React.ReactNode }) {
  return children;
}
